pub mod models;
pub mod traits;

use models::trader::{buyer::Customer, seller::Casher};

// pub fn trade(seller: Casher, buyer: Customer) {
//     let total_price = buyer.cart.total_price();
//     seller.pay(total_price);
// }
